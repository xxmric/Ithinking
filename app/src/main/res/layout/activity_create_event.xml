<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/myevent_bg"
    tools:context=".activity.CreateEventActivity">

    <!-- 第一步：在布局文件中添加一个编辑框组件和一个手势组件 -->
    <EditText
        android:id="@+id/et_createEvent"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="60dp"
        android:layout_marginTop="250dp"
        />
    <android.gesture.GestureOverlayView
        android:id="@+id/gesture"
        android:layout_width="500dp"
        android:layout_height="250dp"
        android:layout_alignParentBottom="true"
        android:gestureStrokeType="multiple"></android.gesture.GestureOverlayView>
</RelativeLayout>